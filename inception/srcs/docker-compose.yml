version: "3"

services:
    nginx:
        build: ./requirements/nginx
        restart: always
    mariadb:
        build: ./requirements/mariadb
        restart: always
    wordpress:
        build: ./requirements/wordpress
        restart: always
